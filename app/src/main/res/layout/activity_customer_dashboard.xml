<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Search Bar -->
    <!-- Replace with your search icon drawable -->

    <!-- RecyclerView for listing businesses -->

    <EditText
        android:id="@+id/searchBar"
        android:layout_width="399dp"
        android:layout_height="74dp"
        android:layout_alignParentStart="true"
        android:layout_marginStart="0dp"
        android:layout_marginLeft="80dp"
        android:layout_marginTop="80dp"
        android:layout_marginEnd="8dp"
        android:layout_marginRight="8dp"
        android:layout_marginBottom="8dp"
        android:drawableLeft="@drawable/search_24"
        android:hint="Search businesses"
        android:inputType="text"
        android:padding="12dp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/businessRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/searchBar"
        android:layout_marginTop="1dp" />


    <EditText
        android:id="@+id/appoitments"
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:layout_above="@+id/appoitmentsRecyclerView"
        android:layout_below="@+id/businessRecyclerView"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="62dp"
        android:padding="12dp"
        android:text="my appoitments" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/appoitmentsRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/businessRecyclerView"
        android:layout_marginTop="45dp" />




    <!-- Other UI elements here -->

</RelativeLayout>